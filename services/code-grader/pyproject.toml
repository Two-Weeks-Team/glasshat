[project]
name = "glasshat-code-grader"
version = "0.1.0"
description = "Glasshat code grader: static repo heuristics -> RepoFacts"
requires-python = ">=3.12"
dependencies = [
    "glasshat-shared",
    "glasshat-agents",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/glasshat"]

[tool.uv.sources]
glasshat-shared = { workspace = true }
glasshat-agents = { workspace = true }
